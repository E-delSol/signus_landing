# Tasks: Fix redundant hero CTA

## Batch 1 — Fix
- [x] T1.1 Change "Conocer Signus" `href` from `#como-funciona` to `#que-es` in the index.html hero section
- [x] T1.2 Verify the `#que-es` section exists and is the intended "Qué es" destination

## Batch 2 — Verify
- [x] T2.1 `npm run build` passes; both hero CTAs point to distinct anchors
